- if error.errors.any?
  #error_explanation.alert.alert-error
    %h4= "#{pluralize(error.errors.count, "error")} prohibited this from being saved:"
    %ul
      - error.errors.full_messages.each do |msg|
        %li= msg
